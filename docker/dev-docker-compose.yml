version: '3.8'
services:
  mc:
    image: itzg/minecraft-server:latest
    container_name: buildin-mc
    environment:
      EULA: 'TRUE'
      TYPE: 'paper'
      VERSION: '1.19.2'
      ONLINE_MODE: 'false'
      MEMORY: '2G'
    ports:
      - '25565:25565'
    volumes:
      - ./plugins:/data/plugins
